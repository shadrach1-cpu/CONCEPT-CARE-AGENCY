// Site settings. Edit these values, then run `node build.js` and re-upload the site.
window.CCA = {
  email: "contact@concepthealthcareagency.co.uk",
  phoneDisplay: "07438 811614",
  phoneTel: "+447438811614",

  // Where the website forms send enquiries. Currently FormSubmit (formsubmit.co, no account needed):
  // enquiries are emailed to the address at the end of this URL.
  // The first ever submission triggers an activation email to that address; click the link in it once.
  // To use another service (e.g. Formspree), paste its endpoint here instead.
  // Set to "" to fall back to opening the visitor's email app.
  formEndpoint: "https://formsubmit.co/ajax/contact@concepthealthcareagency.co.uk"
};
